apiVersion: v1
kind: Service
metadata:
  labels:
    app: webserver
  name: webserver
  namespace: webserver
spec:
  ports:
  - name: http-8080
    port: 8080
    targetPort: 5000
  selector:
    app: webserver
  sessionAffinity: None
  type: ClusterIP
